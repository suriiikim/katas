// Crea una función que devuelva el texto proporcionado en mayúsculas 

// 'toUppercase' convierte una string en mayúsculas.

// 👇🏻 Define aquí tu función
const toUppercase = (text) => { // La function recibe un parametro 'text' que es la string que se va a convertir en mayus. 
                               // Se llama al metodo 'toUppercase()' en parametro 'text' return 
  return text.toUppercase();  // return el resultado.
};
// 👆🏻

toUppercase('hola que tal'); // HOLA QUE TAL 
toUppercase('me llamo Carlos'); // ME LLAMO CARLOS

console.log (toUppercase('hola que tal'));
console.log (toUppercase('me llamo Carlos'));
