const user = { //'user' es el objeto
  name: 'Juan', // propiedad del objeto 1
  age: 36, // propiedad del objeto 2
};

// Añade una propiedad "city" al objeto user con el valor que quieras
user.city = 'Valencia';

// Muestra por consola el nombre del user
console.log (user.name); // Se usa la notación de puntos
// Muestra por consola la edad del user
console.log (user.age);

// y muestra el objeto por consola

console.log (user);
